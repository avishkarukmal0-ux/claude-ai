# 2026-09-16 — M3 & M4 push: migration import · worker view · suggest-to-owner

Back to [[Work-Log]] · Related: [[Implementation-Roadmap]] · [[Features-To-Build]]

## Goal
"Focus on the M's" — build the remaining M-items that need no backend/shop.

## What shipped
- **M4 — Migration/onboarding CSV import** (the switching wedge): `csv.js` parser + column mapping; `importProducts()` (bulk, skips duplicate barcodes, persists synchronously); `ImportView` (upload/paste + sample + preview + import), reachable from the Stock header + empty state. **Bug fixed:** persistence was inside a setState updater that got dropped when the screen unmounted on done → now persists synchronously.
- **M3 — Worker "Staff view" + suggest-to-owner loop**: `suggestionsStore`; `WorkerBoard` (expiring + low-stock with one-tap Stock-this/Mark-down flags + note-to-owner); `SuggestionsInbox` ("From the team", tick done / clear); open-count badge on the inbox tile. New core modules `worker` + `suggestions`.

## Verification (headless Chromium @ 390×844)
- Import: sample of 2 → preview 2 → Import → persisted to storage + shown in Stock.
- Worker loop: flag low-stock → badge "1" on the team tile → inbox shows "Stock this — Milk 2L". No page errors.

## Milestone status now
- **M0 ✅ · M1 ✅ · M3 ✅ (front-end)** · **M4 🟡 mostly** (only demand forecasting left — needs sales history → post-backend) · **M2 🟡** (Owner glance ✅, real MTD figures need backend) · **M5 ❌** (needs a real shop).
- **What's genuinely left is backend-blocked (real MTD, sync, demand forecast) or shop-blocked (M5 field validation).** The local-first build is essentially complete.

## Commits
- feat(app): migration/onboarding CSV import (M4 switching wedge)
- feat(app): worker Staff view + suggest-to-owner loop (M3)
- docs: sync roadmap M3/M4 status
